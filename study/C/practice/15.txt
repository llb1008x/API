15、把一个字符串数组倒置过来
#include<string.h>
#define MAX 100
reverse(const char *in, int len,char *out)
{

}

void main()
{
    char A[MAX] = {0};
    char input[] = "abcdefg";
    int length = strlen(input);
    reverse(input, length, A);
}